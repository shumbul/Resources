Add reusable animated flow-map and use it in 4 fitting places

Made the loved agent-loop style reusable and dropped it where a flow helps.

- New functions/flow.js: a reusable animated pipeline. One attribute
  (data-flow with "Label + emoji" stages) renders nodes connected by a track
  with direction arrows and a glowing packet that travels the path (SVG + SMIL,
  ARIA-labeled, respects prefers-reduced-motion). Wired into site.js (versioned).
- Applied to the most fitting spots:
  * Computer Networks deep dive: what happens when you visit a URL (replaced the
    old static flow)
  * Cloud and DevOps: the CI/CD pipeline (Push, Build, Test, Deploy, Live)
  * Full-Stack path: one request through the stack (Browser to Database to Response)
  * Git guide: the core workflow (Working dir, Staging, Local repo, Remote)
- Validated: 4 pages WCAG AA 0 violations (desktop + mobile), 0 overflow, all
  packets animate, HTMLHint 40 files clean

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>